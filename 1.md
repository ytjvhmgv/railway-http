手动创建 VLESS→HTTP 代理项目（JustRunMy）
你需要准备
1. 本地项目文件
目录：
C:\Users\Lenovo\Desktop\code\railway-vless-http
至少要有：
Dockerfile
entrypoint.sh
有更好：
README.md
.env.example
.dockerignore
.gitignore
railway.toml
parse_link.py
2. 三个关键信息
PROXY_USER=你的用户名
PROXY_PASS=你的密码
VLESS_LINK=完整 vless:// 链接
3. 确认上游 VLESS 本身能用
先在你自己电脑客户端里测通，再部署到云上。
方式 A：Git 部署（推荐，你已成功）
第 1 步：打开 JustRunMy
登录 JustRunMy.App（按你实际地址）
进入控制台
第 2 步：创建应用
点 Create application / 添加应用
选择 Use Git for deployment / Git 部署
不要选 “Existing Docker Image + ghcr.io”（你之前这个会 Authorization failed）
第 3 步：拿到推送命令
页面会给出类似：
git push -u https://用户名:令牌@justrunmy.app/git/xxxxx HEAD:deploy
点 Copy 复制整行。
这行里有部署令牌，不要发到公开群。

第 4 步：在电脑准备代码
打开 CMD 或 PowerShell：
cd C:\Users\Lenovo\Desktop\code\railway-vless-http
如果还没有 git 仓库：
git init -b main
git add Dockerfile entrypoint.sh .dockerignore .gitignore README.md .env.example parse_link.py railway.toml .github
git commit -m "init vless http proxy"
如果已有仓库，改完文件后：
git add .
git commit -m "update"
第 5 步：推送到 JustRunMy
把页面复制的命令贴上运行，例如：
git push -u https://xxxx:yyyy@justrunmy.app/git/r_xxxxx HEAD:deploy
成功时大致会看到：
Starting image build
Docker: Step 1/12 : FROM alpine:3.20
...
Successfully built ...
Image build succeeded
Application created
Application restarted
如果出现 “image built on a different server...”：
等 1 分钟
再执行一次空提交推送：
git commit --allow-empty -m "retry"
git push https://xxxx:yyyy@justrunmy.app/git/r_xxxxx HEAD:deploy
第 6 步：回网页填环境变量
打开刚创建的应用，进入 Environment / 环境变量，添加：
PORT=8080
PROXY_USER=ikun
PROXY_PASS=你的强密码
VLESS_LINK=vless://完整链接
注意：
VLESS_LINK 必须一整行
不要少 vless://
path 里的 %2F 可保持链接原样，程序会自己解码
改完后点 Save，再 Restart
可选：
VLESS_FINGERPRINT=chrome
LOG_LEVEL=warning
第 7 步：确认端口
容器端口：
8080
协议: TCP
JustRunMy 会再映射一个公网端口，例如：
5.161.64.208:50551  ->  容器 8080
第 8 步：看日志确认启动成功
日志应有：
HTTP proxy listening on 0.0.0.0:8080
Upstream VLESS: ...
Xray ... started
如果出现：
failed to dial to x.x.x.x:xxxxx: i/o timeout
那是上游 VLESS 从机房连不上，要换节点，不是上传失败。
第 9 步：本机测试
假设公网是 5.161.64.208:50551：
C:\Windows\System32\curl.exe -x http://ikun:你的密码@5.161.64.208:50551 https://api.ipify.org
成功会直接打印 IP。
再查归属：
C:\Windows\System32\curl.exe -x http://ikun:你的密码@5.161.64.208:50551 https://ipinfo.io
第 10 步：客户端填写
类型: HTTP 代理
地址: 5.161.64.208
端口: 50551
用户名: ikun
密码: 你的密码
完整 URL 形式：
http://ikun:你的密码@5.161.64.208:50551
